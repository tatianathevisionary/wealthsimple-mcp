# Copy this file to `.env` and fill in real values for local dev with telemetry.
# `.env` is gitignored; this file is checked in as documentation.
#
# Usage: `node --env-file=.env build/index.js`  (Node 20+ has native --env-file)
# Or:    `npm run start:telemetry`

# Datadog credentials. Get a fresh API key at:
#   https://app.datadoghq.com/organization-settings/api-keys
#   (or your regional URL, e.g. https://us5.datadoghq.com/...)
DD_API_KEY=
DD_SITE=us5.datadoghq.com

# APM identification (shown in Datadog UI for service search/grouping)
DD_SERVICE=wealthsimple-help-center-mcp
DD_ENV=dev
DD_VERSION=0.1.0

# LLM Observability
# - DD_LLMOBS_ENABLED=true to capture each MCP tool call as an LLM Obs `tool` span.
# - DD_LLMOBS_AGENTLESS_ENABLED=true sends spans directly to Datadog without
#   needing a local Datadog Agent (the typical local-dev setup).
DD_LLMOBS_ENABLED=true
# DD_LLMOBS_ML_APP must match the "Application Name" you typed into the
# Datadog LLM Observability onboarding wizard.
DD_LLMOBS_ML_APP=wealthsimple-mcp
DD_LLMOBS_AGENTLESS_ENABLED=true

# ---------------------------------------------------------------------------
# Privacy redaction (OFF by default — read README → "Privacy & data tracking"
# before deciding). When telemetry is on, the server captures full tool args
# and outputs by default. Args for `search_help_center` include the user's
# free-text query, which can contain personal information if your agent
# forwards user prompts verbatim. Turn these on if that risk applies to you.
# ---------------------------------------------------------------------------
# Skip annotating inputData / outputData on tool spans. Span name, latency,
# error status, throughput are still emitted — only the content is dropped.
# WEALTHSIMPLE_HELP_TELEMETRY_REDACT_IO=true
#
# Strip query strings from the `http.url` tag on Zendesk request spans (the
# `resource` is already path-only). Set this with REDACT_IO for full
# query-text redaction across both span kinds.
# WEALTHSIMPLE_HELP_TELEMETRY_REDACT_URLS=true
