build/
node_modules/
*.tsbuildinfo
.DS_Store
*.log
npm-debug.log*

# Local environment config — never commit secrets (DD_API_KEY etc.)
.env
.env.*
!.env.example

# Local Claude Code tooling — not part of the MCP server
.claude/

# Don't ship the local SDK clone
typescript-sdk/
